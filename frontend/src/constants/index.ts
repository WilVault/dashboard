const constants = {
  ACCESS_TOKEN: 'access_token',

  APP_STATES: {
    INIT:    'INIT',
    LOADING: 'LOADING',
    READY:   'READY',
    ERROR:   'ERROR',
  } as const,
};

export default constants;